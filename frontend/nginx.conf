# frontend/nginx.conf
server {
    listen 80;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Crucial for React/SPA routing: if a file isn't found, fall back to index.html
        try_files $uri $uri/ /index.html;
    }
}